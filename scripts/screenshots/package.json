{
  "name": "claude-starter-screenshots",
  "private": true,
  "type": "module",
  "description": "Screenshot capture. Kept out of ClientApp so a browser download is not part of every client install.",
  "scripts": {
    "capture": "playwright install --with-deps chromium && node capture.mjs"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
